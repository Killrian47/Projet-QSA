<form method="post" action="{{ path('app_echantillons_delete', {'id': echantillon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ echantillon.id) }}">
    <button class="btn">Delete</button>
</form>
